# react-pagination
In this project I created a pagination application. Is an interface pattern that divides content into separate pages

Technologies I used in the project:

* React
* React-Hooks
* React-axios
* Bootstrap

